{
  "name": "touchline-fantasy",
  "private": true,
  "description": "Convenience scripts. server/ and mobile/ each install their own dependencies — see README.md.",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix mobile install",
    "server:dev": "npm --prefix server run dev",
    "server:build": "npm --prefix server run build",
    "server:migrate": "npm --prefix server run prisma:deploy",
    "server:seed": "npm --prefix server run seed",
    "mobile:start": "npm --prefix mobile run start",
    "mobile:web": "npm --prefix mobile run web",
    "mobile:android": "npm --prefix mobile run android",
    "mobile:ios": "npm --prefix mobile run ios"
  }
}
